import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up Free - FightingBooks",
  description: "Create your free FightingBooks account. Get access to 8 popular animals and 28 battle matchups. Generate unlimited illustrated battle books!",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
